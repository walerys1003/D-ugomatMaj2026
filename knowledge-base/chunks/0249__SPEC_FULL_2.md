# 2 — Pola: sygnatura (text, regex VI Nc-e), sąd (select — domyślnie e-Sąd

_source: SPEC_FULL · tags: misc · line 2579 · 248 chars_

Lublin), data nakazu (date picker), data doręczenia (date picker),
   powód nazwa (text), powód adres (textarea), pozwany imię (text),
   pozwany nazwisko (text), pozwany adres (textarea), kwota główna
   (number), odsetki (number), koszty (number)
