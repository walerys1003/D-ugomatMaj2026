# Długomat — base branch

This is the protected `main` branch. All work happens on
`genspark_ai_developer` and lands here via squashed pull requests.

See:
- `docs/PLAN.md` for the 5-tier × 50-task master plan
- `agents/README.md` for the agent orchestra
- `knowledge-base/` for chunked, indexed specifications
